# This is a Databricks asset bundle definition for db_asset_bundle_test.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: db_asset_bundle_test
  uuid: 684c3a06-b6f3-46bd-ba82-09d210ef4156

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-505db2c4-04e8.cloud.databricks.com
      root_path: /Workspace/Users/chiranthkothari96@gmail.com/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      host: https://dbc-505db2c4-04e8.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/chiranthkothari96@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/chiranthkothari96@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: chiranthkothari96@gmail.com
        level: CAN_MANAGE
